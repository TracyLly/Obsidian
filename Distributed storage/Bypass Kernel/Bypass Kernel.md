
内核旁路（Bypass Kernel）的核心思想：绕过操作系统内核这个通用中间层，让应用直接操作硬件。

在网络处理中：让应用程序在用户态直接与网卡硬件交互，处理网络数据包的收发，从而绕过复杂且缓慢的内核网络协议栈，像DPDK、RDMA、XDP、Netmap都是主流的内核旁路技术。
在存储领域中：内核旁路的目标是绕过内核的文件系统和块设备层，像SPDK这样的用户态驱动框架，能绕过存储栈让应用直接操作NVMe硬盘，实现零拷贝、轮询模式的DMA传输。SPDK这样的技术被广泛应用与高性能的分布式数据库和存储系统中，以追求极致的I/O性能。
